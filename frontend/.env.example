# Backend API base URL. Only ever read server-side (Server Components, Server
# Actions, Route Handlers) — the browser never talks to this directly, so it
# deliberately has no NEXT_PUBLIC_ prefix.
API_URL="http://localhost:5001"
